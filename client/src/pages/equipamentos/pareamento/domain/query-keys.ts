/** Chaves React Query usadas na tela de pareamento (invalidação consistente). */
export const pareamentoQueryKeys = {
  lotesRastreadores: ["lotes-rastreadores"] as const,
  lotesSims: ["lotes-sims"] as const,
} as const;
