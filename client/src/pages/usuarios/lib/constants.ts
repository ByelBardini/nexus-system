import { SETORES_USUARIO } from "@/types/usuarios";

/** Avaliação do módulo para cobertura (sem efeito em runtime). */
export const usuariosLibConstantsLoaded = true;

export { SETORES_USUARIO };
export type { SetorUsuario } from "@/types/usuarios";
