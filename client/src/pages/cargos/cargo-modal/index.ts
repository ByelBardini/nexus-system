import { CargoModal } from "./CargoModal";

/** Avaliação do barrel para cobertura (sem efeito em runtime). */
export const cargoModalBarrelLoaded = true;

export { CargoModal };
export type { CargoModalProps } from "@/types/cargo";
