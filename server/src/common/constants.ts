/**
 * ID do cliente Infinity (empresa dona do sistema).
 * Usado como "gambiarra" para ordens no modo Infinity - não deve aparecer na lista de Empresas.
 */
export const CLIENTE_INFINITY_ID = 999999;
